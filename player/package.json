{
  "name": "player",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "npx tsc -b && npm run copy-files",
    "start": "node dist/index.js",
    "copy-files": "copyfiles -u 1 src/**/*.html dist/",
    "dev": "npm run build && npm run copy-files && npm run start",
    "test": ""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "copyfiles": "^2.4.1",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  }
}
